# Copy to .env and fill in. The Makefile and `docker compose` read it.
OPENAI_API_KEY=sk-...

# Defaults match docker-compose.yml — override only if you run infra elsewhere.
MONGO_URL=mongodb://localhost:27017
REDIS_URL=redis://localhost:6379
MONGO_DATABASE=eywa_starter
SERVICE_NAME=eywa-starter
